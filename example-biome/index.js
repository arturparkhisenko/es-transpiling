import {
  empty,
  generateName,
  getRandomQuote,
  zombie,
  zombieFactory,
} from "./src/index";

export { empty, generateName, getRandomQuote, zombieFactory, zombie };

export default { empty, generateName, getRandomQuote, zombieFactory, zombie };
